# 安全政策

## 支持的版本

| 版本    | 是否支持           |
| ------- | ------------------ |
| 0.9.x   | :white_check_mark: |
| 0.8.x   | :white_check_mark: |
| 0.7.x   | :x:                |
| < 0.7   | :x:                |

## 报告漏洞

**请勿公开提 Issue。** 将以下信息发送至 [2115464137@qq.com](mailto:2115464137@qq.com)：

- 漏洞的清晰描述
- 复现步骤
- 受影响版本
- 潜在影响

我们会在 **48 小时内** 回复。漏洞确认并修复后，我们会发布安全公告并致谢（除非你要求匿名）。

## 响应时间线

| 阶段 | 时限 |
|------|------|
| 确认收到 | 48 小时内 |
| 确认漏洞并评估严重程度 | 5 个工作日内 |
| 发布补丁 | 视严重程度而定（严重：48h，高：7天，中：30天） |
| 公开披露 | 补丁发布后进行 |

## 范围

以下内容属于安全漏洞的受理范围：

- 多 Agent 冲突检测绕过
- 审计日志篡改或哈希链断裂
- RBAC 权限提升
- 绕过 13 种注入检测规则的 Prompt 注入
- 未经授权访问共享 Agent 内存
- 通过 MCP 工具泄露 API Key
- Dashboard 认证绕过

## 生产环境最佳实践

部署 Ahy Governance 到生产环境时：

1. **修改默认凭证** — 演示数据仅供开发使用
2. **启用 HTTPS** — 始终在反向代理后使用 TLS
3. **轮换 API Key** — 设置所有 API Key 的过期时间并定期轮换
4. **启用审计日志** — SHA-256 哈希链的效果取决于日志覆盖范围
5. **保持依赖更新** — 每月运行 `pip list --outdated` 检查
