# Place SSL certificates here:
#   ahyops.cn.pem  — 证书文件（阿里云下载的 .pem）
#   ahyops.cn.key  — 私钥文件（申请证书时生成的 .key）
#
# 阿里云免费 DV 证书申请步骤：
#   1. 登录阿里云控制台 → SSL 证书 → 免费证书 → 立即购买
#   2. 填写域名 ahyops.cn，DNS 验证
#   3. 下载 Nginx 格式证书，上传到服务器此目录
#   4. 运行: docker compose up -d
